---
phase: 07-wave-progression-enemy-variety
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - js/scene/combat.js
  - js/scene/waves.js
  - js/scene/weapons.js
  - index.html
autonomous: false
requirements: [ENM-04, ENM-05, ENM-08]

must_haves:
  truths:
    - "Sniper enemies appear at wave 8, stay at extreme range, and fire accurate 3-round kinetic bursts"
    - "Capital enemies appear at wave 10 as a boss encounter, are massive (4-5x Grunt scale), and spawn Grunt minions every ~10 seconds"
    - "Boss waves every 10 waves feature Capital ships with supporting cast of hardest unlocked archetypes"
    - "Capital ships have a dramatic warp-in visual effect at spawn, not just a pop-in"
    - "All 5 archetypes function correctly in a multi-wave playthrough"
  artifacts:
    - path: "js/scene/combat.js"
      provides: "Sniper and Capital AI branches in updateEnemyAI, Capital minion spawning"
      contains: "ETYPE.SNIPER, ETYPE.CAPITAL"
    - path: "js/scene/waves.js"
      provides: "Boss wave composition logic"
      contains: "isBoss"
    - path: "js/scene/weapons.js"
      provides: "Sniper burst fire function"
      contains: "enemySniperBurst"
  key_links:
    - from: "js/scene/combat.js"
      to: "js/scene/weapons.js"
      via: "Sniper AI calls enemySniperBurst()"
      pattern: "enemySniperBurst"
    - from: "js/scene/combat.js"
      to: "js/scene/combat.js"
      via: "Capital AI calls spawnEnemy() for minions"
      pattern: "spawnEnemy.*ETYPE\\.GRUNT"
    - from: "index.html"
      to: "js/scene/combat.js"
      via: "Warp-in scale ramp uses enemies.auxTimer during first 0.5s"
      pattern: "warpIn|auxTimer"
---

<objective>
Implement Sniper and Capital enemy archetypes, boss wave logic, and Capital warp-in visual effect to complete all 5 enemy types.

Purpose: Complete the enemy variety system with the final two archetypes. Snipers force repositioning through long-range precision fire. Capitals create a strategic ticking clock -- they must be killed quickly or their minion spawning overwhelms the player. Boss waves every 10 waves provide milestone encounters.

Output: Sniper burst fire AI, Capital minion-spawning AI, boss wave composition, Capital warp-in VFX, visual verification of complete Phase 7.
</objective>

<execution_context>
@C:/Users/matth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wave-progression-enemy-variety/07-CONTEXT.md
@.planning/phases/07-wave-progression-enemy-variety/07-RESEARCH.md
@.planning/phases/07-wave-progression-enemy-variety/07-01-SUMMARY.md
@.planning/phases/07-wave-progression-enemy-variety/07-02-SUMMARY.md

<interfaces>
<!-- After Plans 01 and 02, these contracts exist -->

From js/scene/combat.js (after Plan 02):
```javascript
const ETYPE = { GRUNT: 0, SWARM: 1, BOMBER: 2, SNIPER: 3, CAPITAL: 4 };
const ARCHETYPE_HP = [100, 30, 200, 60, 800];
const ARCHETYPE_SCALES = [1.0, 0.6, 1.3, 0.8, 4.5];
enemies.auxTimer // Float32Array -- archetype-specific timing
// Swarm AI: converging rush + ram damage (Plan 02)
// Bomber AI: approach + stop + missile salvo (Plan 02)
// AI constants:
const AI_IDLE=0, AI_ALERT=1, AI_TRANSFER=2, AI_ATTACK=3, AI_DISENGAGE=4, AI_REORBIT=5;
const ACCURACY_NOISE=5.0;
function spawnEnemy(x, y, z, type, assignBody, stationPhase) // returns idx or -1
const freeSlots = []; // enemy free slot list
```

From js/scene/waves.js (after Plan 01):
```javascript
function getWaveDefinition(waveNum) // returns { enemies: [{type, count}], isBoss }
function getArchetypeStats(type, waveNum) // returns per-archetype tuning
let waveNumber;
```

From js/scene/weapons.js:
```javascript
function enemyFireAt(enemyIdx, playerPos, playerVel) // fires 1 kinetic round with noise
const ENEMY_KINETIC_SPEED = 60;
function spawnProjectile(type, px, pz, vx, vz) // type=2 for enemy kinetic
const projFreeSlots = [];
```

From js/scene/missiles.js (after Plan 02):
```javascript
function enemyFireMissile(enemyIdx) // fires missile at player (targetIdx=-1)
```

From index.html:
```javascript
function applyPlayerDamage(amount)
function spawnExplosion(x, y, z, scale)
const flyPos, flyVel, playerState
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sniper burst fire and Capital minion-spawning AI</name>
  <files>js/scene/combat.js, js/scene/weapons.js</files>
  <action>
**weapons.js changes -- Sniper burst fire function:**

1. Add `enemySniperBurst(enemyIdx, playerPos, playerVel)` function:
   - Fires a burst of 3 kinetic rounds (type=2) with reduced accuracy noise
   - For each round (0, 1, 2):
     - Check `projFreeSlots.length > 0` before spawning
     - Lead prediction: same as `enemyFireAt` but with `ACCURACY_NOISE * 0.3` (much more accurate than Grunts)
     - Add slight angular spread between rounds: rotate aim direction by `(round - 1) * 0.03` radians (spread of ~1.7 degrees between rounds)
     - Spawn projectile with same `ENEMY_KINETIC_SPEED` as Grunts
   - Spawn muzzle flash: `spawnExplosion(enemies.posX[enemyIdx], 0, enemies.posZ[enemyIdx], 0.5)`
   - This function fires all 3 rounds in a single call (instant burst, no stagger needed -- at 60 speed the rounds naturally spread due to the angular offset)

**combat.js changes -- Sniper AI:**

2. Add Sniper-specific behavior in `updateEnemyAI()`:

**Sniper AI_IDLE:**
- Wider detection radius: `DETECT_RADIUS * 1.5` (60 units). Snipers spot the player from extreme range.

**Sniper AI_ALERT:**
- Standard 0.5s alert pause (Snipers are methodical, not fast).

**Sniper AI_TRANSFER:**
- When `enemies.type[i] === ETYPE.SNIPER`: use MINIMAL mid-course guidance (GUIDANCE_ACCEL_ENEMY * 0.3). Snipers do NOT aggressively close distance.
- Transition to AI_ATTACK at LONG range: when `dist < 40.0` (Snipers engage from extreme distance, not ATTACK_RANGE of 15)
- If already within 15 units, skip to AI_DISENGAGE (Snipers flee close range)

**Sniper AI_ATTACK:**
- When `enemies.type[i] === ETYPE.SNIPER`:
- Use `enemies.auxTimer[i]` as burst cooldown. Increment by simDt.
- When `auxTimer >= 4.0` (4 second cooldown between bursts):
  - Call `enemySniperBurst(i, playerPos, playerVel)` (with typeof guard)
  - Reset auxTimer to 0
  - Set `enemies.hasFired[i] = 1`
- If player closes to dist < 15, immediately transition to AI_DISENGAGE (Snipers don't fight at close range)
- Apply mild retrograde thrust to maintain distance: if dist < 30, add a small velocity component AWAY from the player (0.5 * simDt * awayDir)
- Keep heading aimed at player for firing orientation

**Sniper AI_DISENGAGE:**
- When `enemies.type[i] === ETYPE.SNIPER`: stronger retrograde (0.5x velocity). Transition to REORBIT at dist > 35 (further than Grunt's 20).

**combat.js changes -- Capital AI:**

3. Add Capital-specific behavior in `updateEnemyAI()`:

**Capital AI_IDLE:**
- Standard detection radius. Capitals are station-keepers that wait for the player.

**Capital AI_ALERT:**
- Longer pause: 1.0s. Capitals are slow to react.

**Capital AI_TRANSFER:**
- When `enemies.type[i] === ETYPE.CAPITAL`: very slow approach. Minimal guidance (GUIDANCE_ACCEL_ENEMY * 0.2).
- Actually, Capitals should NOT transfer. They stay at their station-keeping position. Transition directly from AI_ALERT to AI_ATTACK without a transfer burn. In AI_ALERT for Capitals: when alert pause expires, set `enemies.aiState[i] = AI_ATTACK` directly (skip AI_TRANSFER).

**Capital AI_ATTACK:**
- When `enemies.type[i] === ETYPE.CAPITAL`:
- Station-keep: maintain position near assigned body (same logic as AI_IDLE station-keeping but with heading aimed at player). Copy the station-keeping position code from AI_IDLE into the Capital AI_ATTACK branch.
- **Minion spawning:** Use `enemies.auxTimer[i]` as spawn timer. Increment by simDt. When `auxTimer >= 10.0` (spawn every 10 seconds):
  - Spawn 2-3 Grunt minions (`2 + Math.floor(Math.random() * 2)`)
  - For each minion: check `freeSlots.length > 0` before spawning
  - Spawn position: offset from Capital position by random angle, radius 3.0 units
  - Call `spawnEnemy(mx, 0, mz, ETYPE.GRUNT, -1, 0)` -- minions have no assigned body (-1), they go straight to hunting
  - Set minion AI state directly to AI_ALERT with aiTimer = ALERT_PAUSE (skip idle, immediately engage)
  - Reset auxTimer to 0
  - Spawn small muzzle flash at Capital position for visual hangar bay launch effect
- **Heavy shots:** Every 5 seconds (track with a separate counter, use `hasFired` flag toggled per shot, and aiTimer as the shot timer since it's already incrementing): fire a single kinetic round at the player using `enemyFireAt(i, playerPos, playerVel)` but with reduced accuracy noise. To achieve this: temporarily reduce ACCURACY_NOISE before calling enemyFireAt, then restore. Or better: create a local copy of the fire logic with lower noise. Simplest: just call `enemyFireAt` as-is. The Capital's shots are slow/heavy-damage conceptually but use the same kinetic projectile. The damage difference comes from the player needing to prioritize Capital kills, not from raw per-shot damage.
- Capital does NOT disengage or reorbit. It stays in AI_ATTACK until killed. Remove the disengage condition for Capitals.

**Capital warp-in effect:**
4. When a Capital enemy is spawned (detected by checking `enemies.type[idx] === ETYPE.CAPITAL` right after spawnEnemy in waves.js spawnWave), use `enemies.auxTimer[idx]` to track warp-in age. Set `enemies.auxTimer[idx] = -1.0` (negative = warp-in phase).
5. In `updateInstanceBuffer()`: when packing a Capital with `auxTimer < 0`, apply a scale ramp. The Capital starts at scale 0 and grows to full scale over 0.5 seconds. Compute `warpScale = Math.min(1.0, (0.5 + enemies.auxTimer[i]) / 0.5)` -- this goes from 0 at auxTimer=-0.5 to 1 at auxTimer=0 (but we start at -1.0 so the ramp takes 0.5s from -0.5 to 0). Actually: set auxTimer = -1.0 at spawn. In updateEnemyAI, increment auxTimer by simDt. The scale ramp should map auxTimer from -1.0 to -0.5 as scale 0 to 1. So: `warpScale = clamp((enemies.auxTimer[i] + 1.0) / 0.5, 0, 1)`. During the first 0.5s of life (auxTimer -1.0 to -0.5), Capital grows from 0 to full size. From -0.5 onward, full size. At 0.0, auxTimer becomes the spawn timer for minions.
6. In updateInstanceBuffer, multiply the scale by warpScale for Capitals with auxTimer < 0: `instanceData[base+8] = enemies.scale[i] * warpScale`.
7. Spawn a billboard explosion (large, bright) at the Capital's position when it spawns for the flash effect: call `spawnExplosion(posX, 0, posZ, 3.0)` right after spawning a Capital in spawnWave.

**waves.js changes -- boss wave tuning:**
8. Verify/refine `getWaveDefinition()` boss wave composition:
   - Wave 10: 1 Capital + no other archetypes (solo Capital introduction -- player learns the mechanic)
   - Wave 20: 2 Capitals + Snipers + Bombers supporting cast
   - Wave 30+: 3 Capitals + full archetype mix
   - Capital count formula: `Math.min(3, Math.floor(waveNum / 10))`
   - Supporting cast: fill remaining slots with hardest unlocked archetypes (weighted toward Snipers and Bombers)

**Wave skip cheat for testing:**
9. Add a wave skip cheat in index.html keydown handler: when the player types "999" rapidly (within 1 second), skip to the next wave immediately. Track the last 3 keydown digits and their timestamps. When "999" is detected within 1s, call: kill all current enemies (iterate and removeEnemy), which triggers the wave system to progress. This allows testers to quickly reach wave 8, 10, etc. without playing through every wave.
  </action>
  <verify>
    <automated>Open http://localhost:8000 -- enter nav mode, use 999 cheat to skip to wave 8. Observe Sniper enemies (cyan/teal, needle shapes) spawn at extreme range and fire 3-round bursts. Snipers maintain distance. Use 999 to skip to wave 10. Observe Capital enemy (white/silver, massive cruiser at 4-5x scale) with warp-in scale effect and explosion flash. Capital spawns Grunt minions every ~10 seconds. Capital stays at station-keeping position. No console errors.</automated>
  </verify>
  <done>
- Sniper fires 3-round kinetic bursts from extreme range (40+ units) with high accuracy
- Sniper maintains distance, flees when player approaches within 15 units
- Capital spawns at 4-5x Grunt scale with warp-in scale ramp over 0.5s + explosion flash
- Capital stays at station-keeping position, spawns 2-3 Grunt minions every 10 seconds
- Capital minions immediately engage the player (set to AI_ALERT on spawn)
- Minion spawning checks freeSlots.length to prevent MAX_ENEMIES overflow
- Boss wave 10 = 1 Capital solo, wave 20 = 2 Capitals + support
- Wave skip cheat (999) works for testing
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 7: wave progression system with 5 enemy archetypes, boss waves, and difficulty scaling</what-built>
  <how-to-verify>
1. Open http://localhost:8000 and enter nav mode (click any planet)
2. **Wave 1-2 (Grunts only):** Kill all enemies, verify "WAVE 2" announcement appears after breather, wave 2 spawns with slightly more enemies
3. **Wave 3 (Swarm introduction):** Use 999 cheat or play through. Verify yellow/amber dart-shaped enemies spawn in a group and converge on you from multiple angles. They should deal damage on close proximity (check HP bar)
4. **Wave 5 (Bomber introduction):** Verify purple/magenta bulky enemies approach slowly and fire guided missile salvos. Missiles should track you. Shields should absorb missiles.
5. **Wave 8 (Sniper introduction):** Verify cyan/teal needle-shaped enemies stay at extreme range and fire accurate 3-round bursts. They should flee if you approach.
6. **Wave 10 (Boss wave / Capital introduction):** Verify a massive white/silver cruiser spawns with a dramatic scale ramp + explosion flash. It should spawn Grunt minions periodically. Kill it to end the wave.
7. **Visual variety:** All 5 archetypes should have distinctly different geometry shapes and colors
8. **Off-screen indicators:** Verify indicator chevrons show correct archetype colors
9. **Restart:** Die or press Escape, then re-enter nav mode. Verify wave system resets to wave 1
10. **Performance:** No significant FPS drop through wave 10+
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All 5 enemy archetypes render with distinct geometry and colors
- Each archetype has unique AI behavior:
  - Grunt: standard attack pass + fire + disengage loop
  - Swarm: converging rush + ram damage + fast reactions
  - Bomber: slow approach + missile salvos + flees close range
  - Sniper: extreme range + 3-round burst + maintains distance
  - Capital: station-keep + minion spawn + heavy shots + warp-in
- Wave progression: kill-triggered with breather period and announcement
- Boss waves at wave 10, 20, 30 with Capital ships
- Difficulty scales with wave number (more enemies, harder archetypes)
- Enemy-fired missiles work with PN guidance, shield absorption, player damage
- Capital minion spawning respects MAX_ENEMIES free slots
- Wave system resets cleanly on restart
</verification>

<success_criteria>
- Complete playthrough from wave 1 to wave 10+ without crashes
- All 7 phase requirements (ENM-02 through ENM-09) demonstrably met
- 5 distinct enemy archetypes with unique behavior and visuals
- Boss encounter at wave 10 is a clear milestone challenge
- Difficulty curve feels progressive (early waves easy, later waves require strategy)
</success_criteria>

<output>
After completion, create `.planning/phases/07-wave-progression-enemy-variety/07-03-SUMMARY.md`
</output>
