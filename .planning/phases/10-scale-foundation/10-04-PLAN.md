---
phase: 10-scale-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - js/scene/weapons.js
  - js/scene/missiles.js
  - js/scene/combat.js
  - js/scene/particles.js
  - js/scene/explosions.js
  - js/scene/waves.js
autonomous: false
requirements:
  - SCALE-03
  - SCALE-07

must_haves:
  truths:
    - "Kinetic rounds hit targets at 5,000 km range in under 0.5 seconds"
    - "Enemies are visible at combat range -- grunts at 500 m, capitals at 8 km, player at 10 km scale"
    - "Collision bins cover the full play area (0-120,000 km) without overflow"
    - "Missiles track and detonate at km-appropriate distances"
    - "Enemy AI detection/attack/fire ranges are km-appropriate (~20,000 km detect, ~5,000 km fire)"
    - "Combat feels snappy and responsive at 1.0x real-time"
  artifacts:
    - path: "js/scene/weapons.js"
      provides: "km/s projectile speeds, km hit radii, real-time cooldowns"
      contains: "15000"
    - path: "js/scene/missiles.js"
      provides: "km/s missile speeds, km detonation/blast radii"
      contains: "3000"
    - path: "js/scene/combat.js"
      provides: "km-scale collision bins (BIN_WIDTH=5000, NUM_BINS=24), km AI distances, km archetype scales"
      contains: "BIN_WIDTH_KM"
    - path: "js/scene/particles.js"
      provides: "km/s particle speeds"
      contains: "km"
    - path: "js/scene/explosions.js"
      provides: "km-scale explosion sizes"
      contains: "km"
  key_links:
    - from: "js/scene/combat.js (enemy AI)"
      to: "js/scene/scale.js"
      via: "Uses km-scale detection/attack ranges"
      pattern: "DETECT.*km|ATTACK.*km"
    - from: "js/scene/combat.js (collision bins)"
      to: "js/scene/scale.js"
      via: "Uses BIN_WIDTH_KM and NUM_BINS_KM"
      pattern: "BIN_WIDTH_KM"
    - from: "js/scene/weapons.js"
      to: "js/scene/scale.js"
      via: "Uses km/s speed constants"
      pattern: "KINETIC_SPEED"
---

<objective>
Rebalance all combat constants (weapon speeds, missile parameters, AI distances, collision bins, entity scales, explosion sizes, particle speeds) for km-scale coordinates and 1.0x real-time play. This is the final piece that makes combat functional at the new scale.

Purpose: After Plans 02 and 03, positions are in km and time runs at 1.0x. All combat systems still use abstract-unit constants (speeds of 80 units/s, hit radii of 1.5 units, bin width of 10 units). These must be recalibrated for km scale to produce playable combat that feels "snappy, near-instant at combat range" per CONTEXT.md.

Output: All combat modules operating with km-appropriate constants. Combat is playable.
</objective>

<execution_context>
@C:/Users/matth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scale-foundation/10-CONTEXT.md
@.planning/phases/10-scale-foundation/10-RESEARCH.md
@.planning/phases/10-scale-foundation/10-01-SUMMARY.md
@.planning/phases/10-scale-foundation/10-02-SUMMARY.md
@.planning/phases/10-scale-foundation/10-03-SUMMARY.md

@js/scene/scale.js (all km constants)
@js/scene/weapons.js (projectile speed/lifetime/cooldown/hit-radius constants)
@js/scene/missiles.js (missile speed/thrust/fuel/detonation constants)
@js/scene/combat.js (BIN_WIDTH, NUM_BINS, ARCHETYPE_SCALES, AI distance constants, enemy spawn positions)
@js/scene/particles.js (particle speed/lifetime)
@js/scene/explosions.js (explosion size/duration)
@js/scene/waves.js (spawn positions, station-keeping altitudes)

<interfaces>
<!-- From js/scene/scale.js (Plan 01): -->
```javascript
const BODY_SCALE = 800;
const ORBIT_SCALE = 1316;
const BH_GM_KM = /* ~1.371e12 */;
const PLAYER_SIZE_KM = 10.0;
const CAPITAL_SIZE_KM = 8.0;
const BOMBER_SIZE_KM = 0.65;
const SNIPER_SIZE_KM = 0.6;
const GRUNT_SIZE_KM = 0.5;
const SWARM_SIZE_KM = 0.5;
const BIN_WIDTH_KM = 5000;
const NUM_BINS_KM = 24;
const MAX_RADIUS_KM = 120000;
```

<!-- From RESEARCH.md Combat Rebalancing Reference: -->
```
Weapon Speeds (km/s):
  Kinetic:       ~15,000 km/s  (0.33s to hit at 5,000 km)
  Plasma:        ~30,000 km/s  (0.17s to hit at 5,000 km)
  Enemy kinetic: ~10,000 km/s  (0.5s to hit at 5,000 km)
  Missile:       ~3,000 km/s   (1.67s to hit at 5,000 km)
  Nuke missile:  ~2,500 km/s   (2.0s to hit at 5,000 km)

AI Distances (km):
  DETECT_RADIUS:    ~20,000 km
  ATTACK_RANGE:     ~7,500 km
  FIRE_RANGE:       ~5,000 km
  DISENGAGE_DIST:   ~2,500 km
  REORBIT_DIST:     ~10,000 km
  STATION_KEEP_ALT: ~1,500 km

Hit Radii (km):
  HIT_RADIUS_KINETIC: 500 km
  HIT_RADIUS_PLASMA:  800 km
  MISSILE_DET_RADIUS: 300 km

Blast Radii (km):
  MISSILE_BLAST:  2,000 km
  NUKE_BLAST:     8,000 km

Collision Bins:
  BIN_WIDTH: 5,000 km
  NUM_BINS:  24
```

<!-- From js/scene/combat.js (current values to replace): -->
```javascript
const ARCHETYPE_SCALES = [1.0, 0.6, 1.3, 0.8, 4.5];
// Grunt=1.0, Swarm=0.6, Bomber=1.3, Sniper=0.8, Capital=4.5
// These are relative to the base ship model size.
// At km scale, the base model needs to map to GRUNT_SIZE_KM (500m)
// So ARCHETYPE_SCALES should produce: Grunt=0.5km, Swarm=0.5km, Bomber=0.65km, Sniper=0.6km, Capital=8km

const BIN_WIDTH = 10.0;
const NUM_BINS = 20;

// AI distances (in abstract units, embedded in updateEnemyAI):
// DETECT_RADIUS = 40, ATTACK_RANGE = 15, FIRE_RANGE = 10, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebalance weapon, missile, and entity constants for km scale</name>
  <files>js/scene/weapons.js, js/scene/missiles.js, js/scene/combat.js, js/scene/particles.js, js/scene/explosions.js</files>
  <action>
    Update all combat constants from abstract units to km-scale values. Use the RESEARCH.md recommended starting values as the baseline. All values should be declared as named constants at the top of each file (following the existing pattern).

    **js/scene/weapons.js -- replace constants (lines 4-18):**
    ```
    KINETIC_SPEED:       15000 (km/s -- arrives at 5,000 km in 0.33s)
    KINETIC_LIFETIME:    2.0 (seconds -- 30,000 km max range)
    KINETIC_COOLDOWN:    0.8 (seconds -- slightly faster at real-time)
    KINETIC_BURST_COUNT: 5 (unchanged)
    KINETIC_BURST_DELAY: 0.05 (unchanged)
    PLASMA_SPEED:        30000 (km/s -- near-instant)
    PLASMA_MAX_RANGE:    60000 (km -- ~2s travel time)
    PLASMA_FADE_START:   40000 (km -- proportional to max range)
    PLASMA_COOLDOWN:     2.0 (seconds)
    HIT_RADIUS_KINETIC:  500 (km -- generous for gameplay)
    HIT_RADIUS_PLASMA:   800 (km -- slightly larger)
    KINETIC_DAMAGE:      15 (unchanged -- damage is not scale-dependent)
    PLASMA_DAMAGE:       35 (unchanged)
    ENEMY_KINETIC_SPEED: 10000 (km/s -- slightly slower than player)
    ```

    Also update any projectile render sizes -- projectile visual size needs to be visible at km-scale camera distances. A kinetic round might render as a 50-100 km line/trail for visibility (even though the physical hit radius is 500 km). Adjust the vertex data in renderProjectiles accordingly.

    **js/scene/missiles.js -- replace constants (lines 4-16):**
    ```
    MISSILE_THRUST:           2000 (km/s^2)
    MISSILE_NAV_GAIN:         3.0 (unchanged -- dimensionless proportional gain)
    MISSILE_DET_RADIUS:       300 (km -- proximity fuse)
    MISSILE_FUEL_REGULAR:     5.0 (seconds -- ~15,000 km powered range)
    MISSILE_FUEL_NUKE:        8.0 (seconds -- ~24,000 km powered range)
    MISSILE_COAST_DURATION:   1.5 (seconds -- unchanged)
    MISSILE_SPEED:            3000 (km/s -- initial velocity)
    NUKE_MISSILE_SPEED:       2500 (km/s)
    MISSILE_COOLDOWN_REGULAR: 3.0 (seconds -- unchanged)
    MISSILE_COOLDOWN_NUKE:    8.0 (seconds -- unchanged)
    MISSILE_HALF:             [15, 4, 4] (km -- missile visual size, visible at combat range)
    ```

    Also update missile blast radius and nuke blast radius if defined in missiles.js:
    ```
    MISSILE_BLAST_RADIUS: 2000 (km)
    NUKE_BLAST_RADIUS:    8000 (km)
    ```

    **js/scene/combat.js -- update ARCHETYPE_SCALES and AI distances:**

    ARCHETYPE_SCALES needs to map to the km sizes from scale.js. The base model is the grunt ship mesh. The instance shader scales by this factor. Currently Grunt=1.0x, Capital=4.5x. At km scale, the base model size (in the vertex shader) must correspond to GRUNT_SIZE_KM (0.5 km). Then:
    - Grunt: 1.0 (0.5 km)
    - Swarm: 1.0 (0.5 km -- bumped from 0.6 to meet 500m floor)
    - Bomber: 1.3 (0.65 km)
    - Sniper: 1.2 (0.6 km)
    - Capital: 16.0 (8.0 km -- 16x grunt, per CONTEXT.md)

    Update `ARCHETYPE_SCALES = [1.0, 1.0, 1.3, 1.2, 16.0]`

    The base enemy model scale (in the vertex shader or JS model setup) needs to produce 0.5 km when ARCHETYPE_SCALES[GRUNT] = 1.0. Check the current enemy model vertex scale and adjust the base uniform or vertex data so that scale=1.0 corresponds to 0.5 km. The SHIP_HALF in nav.js (player ship half-extents) also needs updating: SHIP_HALF should produce a ~10 km ship.

    **AI distance constants** (embedded in updateEnemyAI function body):
    Find all distance thresholds in the AI state machine and update:
    ```
    DETECT_RADIUS:    20000 km (triggers alert)
    ATTACK_RANGE:     7500 km (transition to attack)
    FIRE_RANGE:       5000 km (weapons free)
    DISENGAGE_DIST:   2500 km (too close, break off)
    REORBIT_DIST:     10000 km (safe to circularize)
    STATION_KEEP_ALT: 1500 km (hover near planet)
    ACCURACY_NOISE:   2000 km (lead prediction scatter)
    ```

    **js/scene/particles.js:**
    Update particle speed/size for km scale. Current particle speed is ~10-20 units/s. At km scale, impact particles should spray at ~500-2000 km/s with appropriate visual size.

    **js/scene/explosions.js:**
    Update EXPLOSION_BASE_SIZE (currently 2.0 abstract units) to km scale. A grunt explosion (~500m ship) should produce a ~1000 km visual explosion. Capital explosion: ~4000 km. Nuke: handled by the volumetric shader.

    **CRITICAL:**
    - Weapon hit radii and projectile sizes are tuned for gameplay feel, NOT derived from scale factor (per CONTEXT.md locked decision)
    - These are starting values -- they may need tuning for feel during the checkpoint
    - Damage values are scale-independent (HP pools don't change)
    - Cooldowns and durations in seconds are mostly unchanged (real-time units)
  </action>
  <verify>
    <automated>Open tests.html -- all tests pass (SCALE-07 bin test validates BIN_WIDTH_KM and NUM_BINS_KM). Open index.html, enter nav mode -- combat is functional with km-scale speeds.</automated>
  </verify>
  <done>All weapon speeds in km/s, all distances in km, all sizes in km. Collision bins cover 0-120,000 km. AI distances km-appropriate. Combat compiles and runs without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Recalibrate collision bins and spawn system for km scale</name>
  <files>js/scene/combat.js, js/scene/waves.js</files>
  <action>
    Update the radial collision bin system and wave spawn system for km-scale distances.

    **js/scene/combat.js -- collision bins (lines 151-170):**
    Replace BIN_WIDTH and NUM_BINS with km-scale values from scale.js:
    ```javascript
    const BIN_WIDTH = BIN_WIDTH_KM;   // 5000 km per bin
    const NUM_BINS = NUM_BINS_KM;     // 24 bins, covers 0-120,000 km
    ```

    The binCounts and binEntities arrays must be resized:
    ```javascript
    const binCounts = new Uint8Array(NUM_BINS);
    const binEntities = new Uint16Array(NUM_BINS * MAX_PER_BIN);
    ```

    The `rebinEntities()` function uses `Math.sqrt(posX^2 + posZ^2)` for radial distance -- this now produces km values since enemy positions are in km. The bin formula `floor(r / BIN_WIDTH)` will correctly bin at km scale with the new constants.

    `getCollisionCandidates()` uses bin radius to find nearby entities. The radius parameter (from weapon hit radii) is now in km. Verify the bin lookup logic: it checks bins from `floor((r - hitR) / BIN_WIDTH)` to `floor((r + hitR) / BIN_WIDTH)`. With BIN_WIDTH=5000 and typical hit radii of 500-800 km, this searches 1-2 bins. Correct behavior.

    **js/scene/waves.js -- spawn positions:**
    Enemy spawns currently use abstract-unit orbit radii. Since flyPos and enemy positions are now in km, spawn positions must also be in km:
    - Spawn orbit radii: use `planetData[i].oR * ORBIT_SCALE` for planet-relative spawns
    - Station-keeping altitude: use km value (~1500 km from planet surface)
    - Spawn distance from player: should be outside detection range (~20,000+ km) but within the play area

    Find all spawn position computations in waves.js and multiply abstract radii by ORBIT_SCALE. If spawns reference planet positions, use `planetPosKm()` from scale.js.

    **Verify bin coverage:**
    Neptune orbit is at 113,176 km. BIN_WIDTH=5000, NUM_BINS=24 covers 0 to 120,000 km. 113,176 / 5000 = bin 22 (of 0-23). Correct -- Neptune enemies fit within bins.

    **Entity position arrays:**
    Enemy positions (enemies.posX/posZ) are Float32Array. At km scale, the largest position is ~113,000 km which has float32 precision of ~8m. This is adequate for collision detection (smallest hit radius is 300 km). However, for SIMULATION accuracy (integrating small velocity changes), consider whether enemies.posX/posZ should be Float64Array.

    Decision: Keep Float32Array for enemy positions. Enemies use simplified BH-only gravity with relatively large velocity changes per frame. The 8m precision at Neptune orbit is negligible compared to 500m+ entity sizes. CRR handles the render precision. If simulation drift is observed, upgrade to Float64Array in a future fix.
  </action>
  <verify>
    <automated>Open tests.html -- SCALE-07 test validates BIN_WIDTH_KM and NUM_BINS_KM. Open index.html, enter nav mode -- enemies spawn, collision detection works, waves progress.</automated>
  </verify>
  <done>Collision bins cover 0-120,000 km without overflow. Spawn positions use km coordinates. Wave system functions at km scale. No entities pile up in overflow bin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete km-scale combat system</name>
  <files>N/A</files>
  <action>
    Human verification of the full Phase 10 integration: CRR rendering (no jitter), log depth (no Z-fighting), Keplerian orbits (Jupiter ~60s), real-time combat (no bullet time), and rebalanced weapon/missile/AI constants for km distances.

    Steps to verify:
    1. Open http://localhost:8000/tests.html -- ALL tests should show PASS (existing shader invariants + new scale tests)
    2. Open http://localhost:8000/ -- normal black hole scene should look identical to before (no visual changes)
    3. Enter nav mode (click "Enter Nav Mode"):
       a. Jupiter should orbit in approximately 60 seconds (time one full revolution)
       b. Navigate to different orbital radii -- no visible jitter on ship or enemies at any distance
       c. Look at entities at different depths -- no Z-fighting/flickering
       d. Fire weapons at enemies -- kinetic rounds should feel near-instant at close range, plasma even faster
       e. Launch missiles -- should track and detonate at appropriate distances
       f. Combat should feel responsive and "snappy" at real-time speed (no bullet time sluggishness)
       g. Enemies should have varied sizes: small grunts (500m), imposing capitals (8km), player ship (10km)
    4. Exit nav mode -- scene should return to normal
    5. Check: Is combat speed balanced? Are projectiles visible? Can you dodge enemy fire? Report any issues.
  </action>
  <verify>User confirms combat is playable or reports specific issues</verify>
  <done>User types "approved" if combat feels good, or describes specific issues (too fast/slow, invisible projectiles, jitter, etc.)</done>
</task>

</tasks>

<verification>
1. tests.html -- all tests pass including SCALE-07 collision bin validation
2. Kinetic rounds arrive at 5,000 km range in under 0.5 seconds (snappy feel)
3. Enemies spawn at km-scale positions and are visible at combat range
4. Collision detection works -- projectiles hit enemies, missiles detonate
5. Wave progression still works -- killing enemies triggers next wave
6. Capital ships are visibly imposing (16x grunt size)
7. No performance regression from collision bin resize
</verification>

<success_criteria>
- All combat constants operate in km units (SCALE-03 entity sizes, SCALE-07 collision bins)
- Combat feels "snappy, near-instant at combat range" per CONTEXT.md
- Collision bins cover full play area without overflow
- Wave spawning works at km scale
- Player confirms combat is playable via human-verify checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/10-scale-foundation/10-04-SUMMARY.md`
</output>
