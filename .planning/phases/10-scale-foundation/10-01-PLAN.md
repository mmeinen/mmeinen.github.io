---
phase: 10-scale-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/scene/scale.js
  - tests.html
autonomous: true
requirements:
  - SCALE-01
  - SCALE-02
  - SCALE-03
  - TIME-02

must_haves:
  truths:
    - "scale.js exports all km-scale constants (body sizes, orbit radii, archetype sizes, BH_GM_KM)"
    - "BH_GM_KM is derived from Kepler's third law at Jupiter's 60s orbit, not hardcoded"
    - "tests.html validates scale constants and Kepler derivation automatically"
    - "Existing tests.html shader invariant tests still pass unchanged"
  artifacts:
    - path: "js/scene/scale.js"
      provides: "All km-scale constants for the combat/nav system"
      contains: "BODY_SCALE"
    - path: "tests.html"
      provides: "Automated validation of scale constants and Kepler derivation"
      contains: "SCALE-01"
  key_links:
    - from: "js/scene/scale.js"
      to: "planetData (index.html)"
      via: "ORBIT_SCALE * oR derivation"
      pattern: "ORBIT_SCALE"
    - from: "tests.html"
      to: "js/scene/scale.js"
      via: "script src import"
      pattern: "scale\\.js"
---

<objective>
Create the scale constants module (js/scene/scale.js) that serves as the single source of truth for all km-scale values, and extend tests.html with automated validation tests for scale constants and Keplerian derivation.

Purpose: This is the foundation module that every subsequent plan imports. Getting the constants right (and tested) prevents cascading errors in CRR, orbital mechanics, and combat rebalancing.

Output: js/scene/scale.js with all exported constants, tests.html with 5+ new scale validation tests.
</objective>

<execution_context>
@C:/Users/matth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scale-foundation/10-CONTEXT.md
@.planning/phases/10-scale-foundation/10-RESEARCH.md

@js/scene/nav.js (lines 10-11: BH_GM=400, PLANET_GM_K=50)
@js/scene/combat.js (lines 151-152: BIN_WIDTH=10, NUM_BINS=20)
@tests.html (existing shader invariant tests)
@index.html (planetData array with oR, ph, sp, radius per planet)

<interfaces>
<!-- Key data the executor needs from the existing codebase -->

From index.html (planetData array, approximate line 430+):
```javascript
// Planet data used by shader and WASM -- DO NOT MUTATE these values
const planetData = [
  { oR: 38.0, ph: 0.0,   sp: 0.018, radius: 2.5,  name: 'Jupiter', href: 'minesweeper.html' },
  { oR: 52.0, ph: 2.094, sp: 0.012, radius: 2.0,  name: 'Saturn',  href: 'pong.html' },
  { oR: 68.0, ph: 4.189, sp: 0.007, radius: 1.5,  name: 'Uranus',  href: 'edmund_game/index.html' },
  { oR: 86.0, ph: 1.047, sp: 0.005, radius: 1.4,  name: 'Neptune', href: 'beatrix_game/index.html' },
  { oR: 28.0, ph: 3.5,   sp: 0.025, radius: 0.54, name: 'Venus',   href: 'fps/index.html' },
  { oR: 33.0, ph: 5.5,   sp: 0.021, radius: 0.6,  name: 'Earth',   href: 'freecell/index.html' },
  { oR: 45.0, ph: 1.5,   sp: 0.015, radius: 1.3,  name: 'Mars',    href: 'cat_invasion/index.html' },
];
```

From tests.html (existing test pattern -- regex extraction from fetched index.html):
```javascript
// Tests use fetch('index.html').then(text => { ... regex extraction ... })
// Pattern: extract constants, validate invariants, report PASS/FAIL in DOM
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create js/scene/scale.js with all km-scale constants</name>
  <files>js/scene/scale.js</files>
  <behavior>
    - BODY_SCALE = 800 (abstract radius -> km)
    - ORBIT_SCALE = 1316 (abstract oR -> km orbit radius)
    - BH_DIAMETER_KM = 20000, BH_RADIUS_KM = 10000
    - Planet diameters in km: Jupiter=4000, Saturn=3200, Uranus=2400, Neptune=2240, Mars=2080, Earth=960, Venus=864
    - Orbit radii in km: Venus=36848, Earth=43428, Jupiter=50008, Mars=59220, Saturn=68432, Uranus=89488, Neptune=113176
    - BH_GM_KM derived from Kepler: (4*pi^2 * 50008^3) / 60^2 -- should be approximately 1.371e12
    - JUPITER_ORBIT_KM = 50008, JUPITER_PERIOD_S = 60.0
    - Archetype sizes: PLAYER_SIZE_KM=10, CAPITAL_SIZE_KM=8, BOMBER_SIZE_KM=0.65, SNIPER_SIZE_KM=0.6, GRUNT_SIZE_KM=0.5, SWARM_SIZE_KM=0.5
    - planetPosKm(p, t) returns [x, 0, z] in km from planetData entry
    - Helper: periodFromOr(oR_abstract) returns period in seconds via T = 60 * (oR/38)^1.5
  </behavior>
  <action>
    Create js/scene/scale.js as a plain JS file (no ES modules -- this project uses script tags). All constants must be declared with `const` at file scope so they are global when loaded via script tag.

    Locked decisions from CONTEXT.md:
    - BODY_SCALE = 800 (from Jupiter diameter: 2.5 * 2 * 800 = 4,000 km)
    - ORBIT_SCALE = 1316 (from Jupiter orbit: 38 * 1316 = 50,008 km)
    - BH_GM_KM derived from Kepler's law: GM = 4*pi^2 * r^3 / T^2 at Jupiter orbit
    - Archetype sizes per CONTEXT.md (500m floor for smallest enemies)

    Include:
    1. Scale factors (BODY_SCALE, ORBIT_SCALE)
    2. Black hole dimensions in km
    3. Planet body sizes in km (as an array parallel to planetData indices: Jupiter=0, Saturn=1, Uranus=2, Neptune=3, Venus=4, Earth=5, Mars=6)
    4. Planet orbit radii in km (as an array parallel to planetData indices)
    5. BH_GM_KM computed from Kepler (NOT hardcoded -- use the formula)
    6. JUPITER_ORBIT_KM, JUPITER_PERIOD_S as named constants
    7. Archetype sizes as named constants (PLAYER_SIZE_KM, CAPITAL_SIZE_KM, etc.)
    8. `planetPosKm(p, t)` function: takes a planetData entry and sim time, returns [x, 0, z] in km. Uses `p.oR * ORBIT_SCALE` for radius and `p.sp * t + p.ph` for angle (angular speed stays in abstract units -- it's angular, not linear).
    9. `periodFromOr(oR)` helper: returns Keplerian period in seconds = 60 * (oR/38)^1.5
    10. Collision bin constants: BIN_WIDTH_KM = 5000, NUM_BINS_KM = 24 (covers 0-120,000 km past Neptune)
    11. MAX_RADIUS_KM = 120000 (world boundary)

    CRITICAL: This file must NOT reference or import planetData -- it receives planetData entries as function parameters. The planetData array lives in index.html and must never be mutated for scale purposes.

    Do NOT include anything from nav.js or combat.js -- this file is pure constants and helpers.
  </action>
  <verify>
    <automated>Open tests.html in browser -- new scale constant tests pass (created in Task 2)</automated>
  </verify>
  <done>js/scene/scale.js exists with all constants and helper functions. BH_GM_KM is formula-derived. No references to planetData global.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests.html with scale constant validation tests</name>
  <files>tests.html</files>
  <behavior>
    - Test SCALE-01: BODY_SCALE=800, planet diameters correct (Jupiter=4000km, etc.)
    - Test SCALE-02: Jupiter orbit at ~50,000 km (ORBIT_SCALE * 38 between 49,500 and 50,500)
    - Test SCALE-03: Archetype sizes correct (PLAYER_SIZE_KM=10, CAPITAL_SIZE_KM=8, GRUNT_SIZE_KM=0.5)
    - Test TIME-02: BH_GM_KM Kepler check -- compute T(Jupiter) from BH_GM_KM and verify ~60s
    - Test SCALE-07: BIN_WIDTH_KM=5000, NUM_BINS_KM=24, covers beyond Neptune orbit (113176 km)
    - All existing shader invariant tests still pass
  </behavior>
  <action>
    Extend tests.html with a new test section for scale.js constants. The existing tests.html pattern fetches index.html text and uses regex extraction. For scale.js tests, add a `<script src="js/scene/scale.js"></script>` tag to tests.html so constants are directly available.

    Add these tests (following the existing DOM-based PASS/FAIL pattern):

    1. **SCALE-01 Body Sizes**: Assert BODY_SCALE === 800. Assert planet diameter array values match expected (Jupiter idx 0 = 4000 km, etc.).
    2. **SCALE-02 Jupiter Orbit**: Assert ORBIT_SCALE * 38 is between 49,500 and 50,500 km.
    3. **SCALE-03 Archetype Sizes**: Assert PLAYER_SIZE_KM === 10, CAPITAL_SIZE_KM === 8, GRUNT_SIZE_KM === 0.5, SWARM_SIZE_KM === 0.5.
    4. **TIME-02 Kepler Derivation**: Independently compute GM = 4*pi^2 * (38*ORBIT_SCALE)^3 / 60^2, compare to BH_GM_KM with <0.1% tolerance. Also verify T(Jupiter) = 2*pi*sqrt(JUPITER_ORBIT_KM^3 / BH_GM_KM) is between 59.5 and 60.5 seconds.
    5. **SCALE-07 Collision Bins**: Assert BIN_WIDTH_KM === 5000, NUM_BINS_KM === 24, and BIN_WIDTH_KM * NUM_BINS_KM > 113176 (covers Neptune orbit).

    Keep all existing shader invariant tests intact and running. The new tests should appear in a separate section labeled "Scale Foundation (Phase 10)".

    IMPORTANT: Read the existing tests.html structure first to match the exact DOM pattern (test container elements, pass/fail CSS classes, result reporting).
  </action>
  <verify>
    <automated>Open tests.html in browser -- all tests (existing + new) show PASS</automated>
  </verify>
  <done>tests.html contains 5 new scale validation tests. All pass. All existing shader invariant tests still pass.</done>
</task>

</tasks>

<verification>
1. js/scene/scale.js exists and declares all constants at global scope
2. BH_GM_KM is computed via formula, not hardcoded
3. tests.html loads scale.js via script tag
4. All 5 new tests pass (SCALE-01, SCALE-02, SCALE-03, TIME-02, SCALE-07)
5. All existing tests.html tests still pass (shader invariants)
</verification>

<success_criteria>
- js/scene/scale.js is the single source of truth for all km-scale constants
- BH_GM_KM produces a ~60s Jupiter orbit when checked via Kepler
- Archetype sizes match CONTEXT.md exactly
- tests.html validates all constants automatically
- Zero regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-scale-foundation/10-01-SUMMARY.md`
</output>
